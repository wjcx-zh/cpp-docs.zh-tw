---
title: C6332
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6332
helpviewer_keywords:
- C6332
ms.assetid: 93d74b3f-4070-4b48-807e-52b1dfee1330
ms.openlocfilehash: 81c4f3c5ea90b9723a5c24ebac1aa0ab7bfcdbaa
ms.sourcegitcommit: 7bea0420d0e476287641edeb33a9d5689a98cb98
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 02/17/2020
ms.locfileid: "77419562"
---
# <a name="c6332"></a>C6332

> 警告 C6332：不正確參數：將零當做 dwFreeType 參數傳遞給 \<函式 > 不允許使用。 這會導致此呼叫失敗

此警告表示將不正確參數傳遞至 VirtualFree 或 VirtualFreeEx。 VirtualFree 和 VirtualFreeEx 都會拒絕 dwFreeType 參數零。 DwFreeType 參數可以是 MEM_DECOMMIT 或 MEM_RELEASE。 不過，不能在相同的呼叫中同時使用 MEM_DECOMMIT 和 MEM_RELEASE 值。 此外，請確定不會忽略 VirtualFree 函數的傳回值。

## <a name="example"></a>範例

下列程式碼會產生此警告，因為傳遞給 VirtualFree 函數的參數無效：

```cpp
#include <windows.h>
#define PAGELIMIT 80

DWORD dwPages = 0;  // count of pages
DWORD dwPageSize;   // page size

VOID f( VOID )
{
  LPVOID lpvBase;            // base address of the test memory
  BOOL bSuccess;
  SYSTEM_INFO sSysInfo;      // system information

  GetSystemInfo( &sSysInfo );
  dwPageSize = sSysInfo.dwPageSize;

  // Reserve pages in the process's virtual address space
  lpvBase = VirtualAlloc(
                         NULL,                // system selects address
                         PAGELIMIT*dwPageSize, // size of allocation
                         MEM_RESERVE,
                         PAGE_NOACCESS );
  if (lpvBase)
  {
    // code to access memory
  }
  else
  {
    return;
  }

  bSuccess = VirtualFree( lpvBase, 0, 0 );
  // code ...
}
```

若要修正這個警告，請修改 VirtualFree 函式的呼叫，如下列程式碼所示：

```cpp
#include <windows.h>
#define PAGELIMIT 80

DWORD dwPages = 0;  // count of pages
DWORD dwPageSize;   // page size

VOID f( VOID )
{
  LPVOID lpvBase;            // base address of the test memory
  BOOL bSuccess;
  SYSTEM_INFO sSysInfo;      // system information

  GetSystemInfo( &sSysInfo );
  dwPageSize = sSysInfo.dwPageSize;

  // Reserve pages in the process's virtual address space
  lpvBase = VirtualAlloc(
                         NULL,                // system selects address
                         PAGELIMIT*dwPageSize, // size of allocation
                         MEM_RESERVE,
                         PAGE_NOACCESS );
  if (lpvBase)
  {
    // code to access memory
  }
  else
  {
    return;
  }

  bSuccess = VirtualFree( lpvBase, 0, MEM_RELEASE );
  // code ...
}
```

在記憶體流失和例外狀況方面，使用 VirtualAlloc 和 VirtualFree 有許多陷阱。 若要避免這類流失和例外狀況問題，請使用C++標準範本程式庫（STL）提供的機制。 其中包括[shared_ptr](/cpp/standard-library/shared-ptr-class)、 [unique_ptr](/cpp/standard-library/unique-ptr-class)和[向量](/cpp/standard-library/vector)。 如需詳細資訊，請參閱[智慧型指標](/cpp/cpp/smart-pointers-modern-cpp)和[ C++標準程式庫](/cpp/standard-library/cpp-standard-library-reference)。

## <a name="see-also"></a>另請參閱

- [VirtualAlloc 方法](/dotnet/framework/unmanaged-api/hosting/ihostmemorymanager-virtualalloc-method)
- [VirtualFree 方法](/dotnet/framework/unmanaged-api/hosting/ihostmemorymanager-virtualfree-method)
