---
title: C6400
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6400
helpviewer_keywords:
- C6400
ms.assetid: 35808969-1d43-41e8-bcda-33635637fb26
ms.openlocfilehash: 6037cc1c58f9463a7ac4d558479dfcd0b94adc40
ms.sourcegitcommit: 7bea0420d0e476287641edeb33a9d5689a98cb98
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 02/17/2020
ms.locfileid: "77419394"
---
# <a name="c6400"></a>C6400

> 警告 C6400：使用 \<函數名稱 > 來執行與常數位串 \<字串名稱 > 的不區分大小寫比較。 在非英文地區設定中產生未預期的結果

此警告表示會以與地區設定相關的方式來執行與常數位串的區分大小寫比較，但明顯地說，與地區設定無關的比較是有意的。

這項缺陷的一般結果是非英文說話地區設定中不正確的行為。 例如，在土耳其文中，".gif" 不會符合 "。GIF ";在越南文中，"LogIn" 不會符合 "LOGIN"。

通常應該使用 `CompareString` 函數來執行字串比較。 若要在 Windows XP 上執行與地區設定無關的比較，第一個參數應該是常數 `LOCALE_INVARIANT`。

## <a name="example"></a>範例

下列程式碼會產生這個警告：

```cpp
#include <windows.h>
int f(char *ext)
{
  // code...
  return (lstrcmpi(ext, TEXT("gif")) == 0);
}
```

若要修正這個警告，請執行與地區設定無關的測試，讓 char * ext 符合「gif」是否忽略大寫/小寫的差異，請使用下列程式碼：

```cpp
#include <windows.h>
int f(char *ext)
{
  // code...
  return (CompareString(
                        LOCALE_INVARIANT,
                        NORM_IGNORECASE,
                        ext,
                        -1,
                        TEXT ("gif"),
                        -1) == CSTR_EQUAL);
}
```

## <a name="see-also"></a>另請參閱

<xref:Microsoft.VisualBasic.CompilerServices.Operators.CompareString%2A>
